```mermaid
   flowchart TD;
   A(start)--int--> B[/inisialisasi variabel m untuk meter, cm untuk centimeter, dan inci untuk inci/];
   B --> C[/masukkan nilai variabel m/];
   C --> D[/kalikan nilai variabel m dengan 100 untuk variabel cm /];
   D --> E[/kalikan nilai variabel m dengan 100 lalu bagi dengan 2,54 untuk variabel inci /];
   E --> F[/masukkan nilai variabel cm/];
   F --> G[/masukkan nilai variabel inci/];
   G --> H[/finish/];